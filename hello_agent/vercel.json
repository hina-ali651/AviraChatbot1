{
  "version": 2,
  "buildCommand": "UV_NO_COMPILE=1 pip install --prefer-binary -r requirements.txt",
  "builds": [
    {
      "src": "./fast.py",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/fast.py"
    }
  ]
}
