{
  "version": 2,
  "buildCommand": "pip install --upgrade pip setuptools wheel",
  "builds": [
    {
      "src": "./fast.py",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/fast.py"
    }
  ]
}
